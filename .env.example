# Pick one: gemini | groq | mock
# mock needs no key at all - use it to build/test your agent before anyone
# has real keys wired up.
LLM_PROVIDER=mock

GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
